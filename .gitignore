# Datos (nunca en git — usar make data para reproducir)
data/raw/
data/processed/

# MLflow — los runs viven aquí localmente, no en git
mlruns/
mlartifacts/
# La DB es el backend de tracking: contiene los runs, así que aplica la misma
# regla. Trackearla metía un diff binario de 2 MB en cada entrenamiento.
mlflow.db

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.env

# Entorno virtual
venv/
.venv/

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# Reports generados (sí se commitean manualmente)
# reports/*.png → excepción: sí queremos comparar visualmente
