# Datos (nunca en git — usar `make data` de cada capítulo para reproducirlos).
# Sin barra inicial, así aplica en cualquier capítulo: ch02-*/data/raw, etc.
data/raw/
data/processed/
data/

# MLflow — los runs viven aquí localmente, no en git
mlruns/
mlartifacts/
# La DB es el backend de tracking: contiene los runs, así que aplica la misma
# regla. Trackearla metía un diff binario de 2 MB en cada entrenamiento.
mlflow.db

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.env

# Entorno virtual
venv/
.venv/

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# Reports generados sí se commitean: queremos comparar las gráficas entre
# versiones, y pesan poco.

# Vercel
.vercel
.env*
